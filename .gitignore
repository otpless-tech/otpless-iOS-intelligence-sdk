.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
.otpless-debug

# NOTE: CLAUDE.md used to be ignored here. It is now a committed constitution — the
# reflection contract this library must not break is not something each contributor
# should have to rediscover locally.
docs/
